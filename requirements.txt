# QLib是可选的，如果需要使用真实数据可以安装
# pip install qlib
# 注意：最新版本为 0.0.2.dev20，不支持 0.9.16
qlib==0.0.2.dev20  # 可选：用于加载真实股票数据

# 核心依赖
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0
pyyaml>=5.4.0
requests>=2.26.0
python-dateutil>=2.8.0
joblib>=1.1.0
Flask>=2.2.0

# 定期数据下载依赖
apscheduler>=3.10.0  # 用于定时任务
